# Directus Service
apiVersion: v1
kind: Service
metadata:
  name: directus
  namespace: wow
  labels:
    app: directus
spec:
  type: ClusterIP
  ports:
    - port: 8055
      targetPort: 8055
      protocol: TCP
  selector:
    app: directus
