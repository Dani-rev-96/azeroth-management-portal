apiVersion: v1
kind: Service
metadata:
  name: db-tunnel-ssh
  namespace: wow
  labels:
    app: db-tunnel-ssh
spec:
  type: ClusterIP
  ports:
    - name: ssh
      port: 2222
      targetPort: 2222
      protocol: TCP
  selector:
    app: db-tunnel-ssh
